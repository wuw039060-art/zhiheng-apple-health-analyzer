# Windows 发布说明

## 成品

- 文件：`release/知衡健康_1.3.2_Windows_x64_离线安装包.exe`
- 版本：1.3.2
- 架构：Windows x64
- 大小：210,057,631 字节
- SHA-256：`C6E75F7374E2D1396E7CFA59AEB18309A3FB4A221BCC0D24547AC0B6095416F4`
- 安装范围：当前 Windows 用户

安装包保留在本机并作为 GitHub Release 附件发布，不进入普通 Git 历史；仓库只提交 `release/SHA256.txt` 和本说明。

安装包使用 NSIS，内置 WebView2 离线运行时。最终用户不需要预装 Node.js、Rust、Visual C++ 开发工具或数据库，也不需要联网下载 WebView2。

## 2026-07-19 版本 1.3.2

- 根据实际截图定向放大数据页与设置页的小字号内容；
- 样本名称提升至 13px、计数 14px、数据质量标签 13px、HealthKit 映射正文 12px；
- 设置项标题提升至 14px、说明 12px，证据库标题 14px、摘要 12px；
- 同步增加表格行高、设置行高和证据卡高度，英文论文标题允许换行显示；
- 前端 20 项测试及 production build 通过。
- 最终发布包静默安装返回 0，注册版本为 1.3.2；主程序与离线 WebView2 DLL 完整，启动后持续响应。

## 2026-07-19 版本 1.3.1

- 活动区从 12 格数据表重构为步数环、移动进度、能量构成和长期步态带；
- 生命体征从化验单式表格改为组合总览、个人范围轨道和多指标结论；
- 睡眠、恢复、活动、生命体征以及每一张趋势图下均新增可展开的交叉分析；
- 图表分析展示有效天数、近期均值、前一窗口、范围、关联指标、解释边界和下一步；
- 建议升级为临床推理式健康管理，包含支持证据、待排原因、分阶段计划、复查节点、停止条件和就医阈值；
- 楷体仅用于标题与关键结论，数字、图表和长正文保留系统黑体；建议正文增至 12–14px，标题约 29px；
- 前端 20 项测试、Rust 4 项测试及 Tauri production build 通过。
- 最终发布包静默安装返回 0，Windows 注册版本为 1.3.1；
- 安装目录包含主程序和 `WebView2Loader.dll`，启动 20 秒后窗口标题正确且保持响应。

## 2026-07-19 版本 1.3.0

- 依据 Vetric 素材的高对比文字、浅色画布、柔和色块和模块化叙事，重构为真正的桌面工作区；
- 将原先 7–9px 的辅助说明统一提升到 10–12px，并同步增加表格、设置项和指标行高度；
- 新增每日、洞察、建议、数据和设置五个页面，以及任意日期切换和无数据日状态；
- 洞察页支持 7/30/90/180/365 天趋势，缺失数据不补线，并提供图表缩放和图片保存；
- 扩展睡眠阶段、心率区间、步行心率、距离、站立、楼层和步态等 HealthKit 聚合；
- 每日状态采用确定性贡献规则、个人 28 天稳健基线、睡眠债和多生命体征交叉验证；
- 支持直接导入 ZIP 或 `export.xml`，以及本地 CSV 导出、按日期/指标删除和全部清除；
- 前端 18 项测试、Rust 4 项测试及 Tauri production build 通过；
- 安装包内置离线 WebView2 和 x64 `WebView2Loader.dll`，不依赖开发服务器。
- 最终发布包静默安装返回 0，卸载注册表版本为 1.3.0；
- 安装目录同时存在 `watch-health-lens.exe` 与 `WebView2Loader.dll`；
- 安装后的应用启动并持续运行 20 秒，窗口标题为“知衡健康”且 `Responding=True`。

## 2026-07-16 验证记录

- Tauri production build 完成，启用 `custom-protocol` 并把 Vite 生产资源编译进应用；
- WebView 历史验证实际页面为 `http://tauri.localhost/`、标题为“知衡健康”，没有访问开发地址 `http://localhost:1420`；
- 应用主程序使用静态 C/C++ 运行库链接，没有 `libunwind.dll` 等额外工具链依赖；
- `WebView2Loader.dll` 作为 x64 安装资源部署到主程序同级目录；
- NSIS 静默安装返回 0；
- NSIS 静默卸载返回 0，卸载注册表项和安装目录均移除；
- 使用最终发布包重新安装返回 0；
- 全新安装后的首次启动持续运行至少 20 秒，主窗口标题为“知衡健康”且正常响应；
- 未出现 `WebView2Loader.dll` 系统错误窗口；
- 前端单元测试 13 项通过，Rust 后端单元测试 2 项通过。

## 2026-07-17 版本 1.1.0

- 依据 Apple Health、WHOOP、Oura 与 Garmin Connect 的公开功能结构重构首页；
- 首页新增今日优先结论和第一条行动建议；
- 新增睡眠、恢复、活动三大支柱，不制造未经验证的综合医学分数；
- 新增生命体征 Health Monitor，对照个人基线展示静息心率、HRV、呼吸、血氧和腕温；
- 状态年龄改为圆环总览，圆环表达数据把握度，详细贡献因子按需展开；
- 新增提示、趋势、状态年龄和隐私方法的页面内导航；
- TypeScript 生产构建和前端 13 项测试通过；
- Windows 静默安装返回 0，注册版本为 1.1.0；
- `WebView2Loader.dll` 正确安装；
- 安装后的 1.1.0 主窗口运行至少 20 秒，标题为“知衡健康”且正常响应。

## 2026-07-17 版本 1.2.0

- 依据 Apple Human Interface Guidelines 的 Simplicity、Hierarchy、Materials、Color 和 Typography 原则重新设计；
- 删除固定侧栏、深色紫色首屏、英文栏目标签和密集小字号卡片；
- 改为 52px 顶部半透明导航、浅色内容层、系统蓝主操作和更大的标题/正文层级；
- 概览、提示、趋势、状态年龄、方法与隐私改为五个独立视图，不再堆在单一长页面；
- 概览只保留今日结论、生命体征、状态年龄摘要和周期指标；
- 状态年龄摘要可进入独立详情视图查看区间、贡献因子和模型边界；
- TypeScript 生产构建和前端 13 项测试通过；
- Windows 静默安装返回 0，注册版本为 1.2.0；
- 原有本机健康数据库得到保留，`WebView2Loader.dll` 正确安装；
- 1.2.0 主窗口运行至少 20 秒且正常响应。

## 2026-07-17 版本 1.2.1

- 修复活动能量、静息能量和 HRV 在趋势图悬浮提示中显示过多浮点小数的问题；
- 同步排查并修复睡眠、静息心率、血氧、呼吸频率、VO₂ max、一分钟心率恢复和日照时间的同类问题；
- 建立统一健康指标格式器，概览、生命体征、趋势图、异常证据和状态年龄明细共用相同精度与单位；
- 能量、日照、步数、锻炼、静息心率、HRV 和心率恢复按整数显示；睡眠与 VO₂ max 保留 1 位，呼吸与血氧保留 1 位，腕温偏移保留 2 位；
- 前端格式化回归测试新增 3 项，前端共 16 项测试通过，Rust 后端 2 项测试通过；
- TypeScript 生产构建与 Windows x64 NSIS 离线安装包构建通过。

## 已知发布边界

当前安装包没有商业代码签名证书，因此 Windows SmartScreen 可能显示“未知发布者”。这不影响程序离线运行，但正式向更多用户分发前建议购买代码签名证书并对安装包签名。

本轮在当前 Windows 环境完成了真实安装验证；尚未在独立的纯净 Windows 10 和 Windows 11 虚拟机矩阵中逐台执行兼容性测试。Tauri 的 Windows 安装器文档说明可使用 WebView2 offline installer 支持离线安装：<https://v2.tauri.app/distribute/windows-installer/>。
